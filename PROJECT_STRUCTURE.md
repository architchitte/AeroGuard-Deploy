# AeroGuard Project Structure

```
AeroGuard/
│
├── 📄 Core Files
│   ├── run.py                    # Development entry point
│   ├── wsgi.py                   # Production WSGI entry point
│   ├── requirements.txt          # Python dependencies
│   ├── .env.example             # Environment template
│   └── .gitignore               # Git ignore rules
│
├── 📦 Application Code (app/)
│   ├── __init__.py              # Flask app factory
│   ├── config.py                # Configuration management
│   │
│   ├── 🤖 models/
│   │   ├── __init__.py
│   │   └── forecast_model.py    # ML Models
│   │       ├── ForecastModel class
│   │       ├── RandomForestRegressor
│   │       ├── XGBRegressor
│   │       └── Ensemble implementation
│   │
│   ├── 🔧 services/
│   │   ├── __init__.py
│   │   ├── forecasting_service.py
│   │   │   └── ForecastingService (Business logic)
│   │   └── data_service.py
│   │       └── DataService (Data management)
│   │
│   ├── 🛠️ utils/
│   │   ├── __init__.py
│   │   ├── validators.py        # InputValidator class
│   │   ├── preprocessors.py     # DataPreprocessor class
│   │   └── error_handlers.py    # Custom exceptions
│   │
│   └── 📡 routes/
│       ├── __init__.py
│       ├── health.py            # Health endpoints
│       │   ├── GET /health
│       │   ├── GET /health/ready
│       │   └── GET /health/live
│       ├── forecast.py          # Forecast endpoints
│       │   ├── POST /forecast
│       │   ├── GET /forecast/{location_id}
│       │   └── GET /forecast/{location_id}/current
│       └── model.py             # Model endpoints
│           ├── POST /model/train
│           ├── POST /model/save
│           ├── POST /model/load
│           ├── GET /model/status
│           └── GET /model/{param}/feature-importance
│
├── 🧪 Testing & Demo
│   ├── quickstart.py            # Demo script
│   └── test_api.py              # API test suite
│
├── 📚 Documentation
│   ├── README.md                # Project documentation
│   ├── DEVELOPMENT.md           # Development guide
│   └── SETUP_SUMMARY.md        # This file
│
├── 🐳 Deployment
│   ├── Dockerfile               # Docker image
│   └── docker-compose.yml       # Docker Compose
│
└── .git/                        # Git repository
```

## API Endpoint Architecture

```
/api/v1/
├── /health (Health Checks)
│   ├── GET /              # General health
│   ├── GET /ready         # Readiness probe
│   └── GET /live          # Liveness probe
│
├── /forecast (Forecasting)
│   ├── POST /              # Generate forecast
│   ├── GET /{location_id}  # Get location forecast
│   └── GET /{location_id}/current  # Current conditions
│
└── /model (Model Management)
    ├── POST /train              # Train model
    ├── POST /save               # Save model
    ├── POST /load               # Load model
    ├── GET /status              # Model status
    └── GET /{parameter}/feature-importance  # Feature importance
```

## Data Flow Architecture

```
┌─────────────────────────────────────────────────────┐
│           HTTP Client / API Consumer                │
└────────────────────────┬────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│         Route Handler (Flask Blueprint)             │
│  • Parse request                                    │
│  • Basic routing                                    │
└────────────────────────┬────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│      Input Validation (Validators)                  │
│  • Validate location_id                            │
│  • Validate days_ahead                             │
│  • Validate data types                             │
└────────────────────────┬────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│      Service Layer (Business Logic)                 │
│  • ForecastingService.generate_forecast()          │
│  • DataService.fetch_historical_data()             │
│  • Orchestrate operations                          │
└────────────────────────┬────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        ▼                ▼                ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ Data         │  │ ML Model     │  │ Preprocessor │
│ Processing   │  │ Predictions  │  │ Features     │
└──────────────┘  └──────────────┘  └──────────────┘
        │                ▼                │
        └────────────────┬────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│         Data Formatting & Response                  │
│  • Format predictions                              │
│  • Add metadata                                    │
│  • JSON serialization                              │
└────────────────────────┬────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│         JSON Response (HTTP 200/400/500)            │
└─────────────────────────────────────────────────────┘
```

## ML Model Pipeline

```
Training Data (X, y)
        │
        ▼
┌─────────────────────────┐
│ Data Preprocessing      │
│ • Normalization         │
│ • Feature scaling       │
│ • Outlier removal       │
└────────────┬────────────┘
             │
   ┌─────────┴─────────┐
   ▼                   ▼
┌──────────────┐  ┌──────────────┐
│ Random       │  │ XGBoost      │
│ Forest       │  │ Regressor    │
│ Regressor    │  │              │
└──────┬───────┘  └──────┬───────┘
       │                 │
       └────────┬────────┘
                ▼
        ┌──────────────┐
        │ Ensemble     │
        │ (Average)    │
        └──────┬───────┘
               │
               ▼
        ┌──────────────┐
        │ Predictions  │
        │ (Clipped)    │
        └──────────────┘
```

## Class Diagram (Key Classes)

```
┌──────────────────────────┐
│   ForecastModel          │
├──────────────────────────┤
│ - models: dict           │
│ - scalers: dict          │
│ - is_trained: bool       │
├──────────────────────────┤
│ + train(X, y_dict)       │
│ + predict(X, param)      │
│ + save(path)             │
│ + load(path)             │
│ + get_feature_importance │
└──────────────────────────┘

┌──────────────────────────┐
│   ForecastingService     │
├──────────────────────────┤
│ - model: ForecastModel   │
│ - preprocessor           │
├──────────────────────────┤
│ + generate_forecast()    │
│ + _format_forecast()     │
│ + _estimate_confidence() │
└──────────────────────────┘

┌──────────────────────────┐
│   DataService            │
├──────────────────────────┤
│ - cache: dict            │
├──────────────────────────┤
│ + fetch_historical_data()│
│ + fetch_current_cond()   │
│ + validate_location()    │
│ + get_location_metadata()│
└──────────────────────────┘

┌──────────────────────────┐
│   InputValidator         │
├──────────────────────────┤
│ (Static Methods)         │
├──────────────────────────┤
│ + validate_location_id() │
│ + validate_days_ahead()  │
│ + validate_forecast_req()│
│ + sanitize_string()      │
└──────────────────────────┘

┌──────────────────────────┐
│   DataPreprocessor       │
├──────────────────────────┤
│ (Static Methods)         │
├──────────────────────────┤
│ + prepare_features()     │
│ + normalize_features()   │
│ + remove_outliers()      │
│ + handle_missing_values()│
│ + scale_to_range()       │
└──────────────────────────┘
```

## Technology Stack

```
┌─────────────────────────────────────────────┐
│           Web Framework                     │
│  Flask 2.3.3 + Flask-CORS                   │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│           ML & Data Processing              │
│  • Scikit-learn 1.3.0                       │
│  • XGBoost 2.0.0                            │
│  • Pandas 2.0.3                             │
│  • NumPy 1.24.3                             │
│  • Statsmodels 0.14.0                       │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│           Production Server                 │
│  Gunicorn 21.2.0                            │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│           Model Persistence                 │
│  Joblib 1.3.1                               │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│           Containerization                  │
│  Docker + Docker Compose                    │
└─────────────────────────────────────────────┘
```

## Configuration Hierarchy

```
┌─────────────────────────────────────────┐
│   Base Config (app/config.py)           │
│   - Common settings                     │
│   - Default values                      │
└────────────────┬────────────────────────┘
                 │
     ┌───────────┴──────────┐
     │                      │
     ▼                      ▼
┌──────────────────┐  ┌──────────────────┐
│ Development      │  │ Production       │
│ Config           │  │ Config           │
│ - DEBUG=True     │  │ - DEBUG=False    │
│ - Testing mode   │  │ - Security focus │
└──────────────────┘  └──────────────────┘

Both extend from Base Config and can be
overridden by Environment Variables (.env)
```

## Error Handling Flow

```
HTTP Request
    │
    ▼
Route Handler
    │
    ├─ Missing required field?
    │  └─> ValidationError (400)
    │
    ├─ Invalid data type?
    │  └─> ValidationError (400)
    │
    ├─ Model not trained?
    │  └─> ModelNotTrainedError (400)
    │
    ├─ Data service fails?
    │  └─> DataServiceError (500)
    │
    ├─ Unexpected exception?
    │  └─> Generic error (500)
    │
    └─ Success?
       └─> JSON response (200)

All errors returned as:
{
    "status": "error",
    "message": "descriptive message",
    "code": status_code
}
```

## Deployment Architecture

```
┌─────────────────────────────────────────┐
│      Client (Browser/App)               │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│      Reverse Proxy (nginx)              │
│      - SSL/TLS termination              │
│      - Load balancing                   │
└────────────────┬────────────────────────┘
                 │
     ┌───────────┴──────────┐
     │                      │
     ▼                      ▼
┌──────────────────┐  ┌──────────────────┐
│ Gunicorn         │  │ Gunicorn         │
│ Worker 1         │  │ Worker 2         │
│ (Port 8000)      │  │ (Port 8001)      │
└──────┬───────────┘  └──────┬───────────┘
       │                     │
       └──────────┬──────────┘
                  │
                  ▼
        ┌────────────────────┐
        │ AeroGuard App      │
        │ - Flask app        │
        │ - ML models        │
        │ - Services         │
        └─────────┬──────────┘
                  │
                  ▼
        ┌────────────────────┐
        │ Data Layer         │
        │ - Database         │
        │ - Cache (Redis)    │
        │ - File storage     │
        └────────────────────┘
```

---

**Complete architecture for AeroGuard - Production-ready Air Quality Forecasting System**
